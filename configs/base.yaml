seed: 42
folds: 5
epochs: 100
batch_size: 4
num_workers: 4
lr: 0.0001
weight_decay: 0.0001
max_grad_norm: 1.0
grad_accum_steps: 1
early_stop_patience: 12
selection_policy: best_composite_early_stop
scheduler: cosine
min_lr: 1.0e-06
cls_lr_mult: 2.0
label_smoothing: 0.05
seg_bce_weight: 0.5
use_uncertainty_weighting: true
lambda_anchor: 0.01
boundary_num_nodes: 64
boundary_hidden_dim: 128
boundary_gnn_layers: 3
boundary_max_offset: 0.08
freeze_medsam: true
unfreeze_medsam_last_n: 1
image_size: 256
medsam_input_size: 1024
medsam_checkpoint_path: checkpoints/medsam_vit_b.pth
architecture_profile: paper
clinical_preprocessing_profile: missing_aware
save_oof_npz: true
keep_fold_checkpoints: true
experiment_name: bua_lel
score_w_seg: 0.3
score_w_cls: 0.7
boundary_radius_ratio: 0.15
peritumor_radius_ratio: 0.4
late_raw_clinical_fusion: true
