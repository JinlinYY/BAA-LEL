# Local model parameters

Place a MedSAM ViT-B checkpoint at `medsam_vit_b.pth`, or configure its path in YAML. SAM and ImageNet backbone experiments have separate checkpoint options. Obtain pretrained parameters from the original projects and follow their terms. Training writes fold-specific checkpoints to the configured output directory. No pretrained or trained weights are included in this repository.
